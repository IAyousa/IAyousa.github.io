# 个人博客（Hexo + Butterfly）

中文个人博客，记录生活、学习、技术三类内容。单站点混排，GitHub 托管，本地写作为主。

## 内容语言

**文章**：
站点的原子单位：一篇 Markdown + front-matter（标题、日期、分类、标签）。
_Avoid_: 博文、日志、帖子

**分类**：
文章的主坐标系，一篇文章恰好属于一个分类。初始只有三个：生活、学习、技术。改名或新增只需改文章 front-matter，不影响永久链接。
_Avoid_: 栏目、板块、频道

**标签**：
文章的自由标注，一篇文章可挂任意多个，不预设词表。

**草稿**：
尚未完成、不想公开的文章，构建时不产出页面，发布后即转为正式文章。
_Avoid_: 草稿箱

**占位文**：
建站初期为验证站点结构而放入各分类的临时文章，可随时删除或改写。

## 站点语言

**源码**：
仓库 `main` 分支里唯一该存在的东西：Markdown 文章、配置、主题依赖声明。构建出来的 HTML 不算源码，永远不提交。
_Avoid_: 博客代码

**构建产物**：
`hexo generate` 生成的静态 HTML，由 Actions 构建后直接发布到 Pages，不落地仓库。

**永久链接**：
一篇文章的终身 URL。标题、日期怎么改它都不变，由 abbrlink 生成的短 ID 保证。
_Avoid_: 地址、链接

**站点身份**：
站名、作者昵称、头像、favicon 的合称。随时可改，与文章内容完全解耦。

**评论**：
读者在某篇文章下的回复，实际存储在 GitHub Discussions 中（经 Giscus），不进博客仓库。
_Avoid_: 留言、留言板

**主题**：
站点的皮肤与布局层（现为 Butterfly，npm 依赖安装）。可整体替换，不影响文章内容。

**vendor 主题**：
把主题源码拷入仓库以魔改的模式，替代 npm 依赖安装。预留的将来动作，触发条件是"开始改主题模板"。
